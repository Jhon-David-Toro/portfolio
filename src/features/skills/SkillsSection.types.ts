import type { ComponentType } from 'react'
import type { IconProps } from '../../design-system/icons/icon.types'

/** A skill-group icon component, matching the shape every icon shares. */
export type IconComponent = ComponentType<IconProps>
