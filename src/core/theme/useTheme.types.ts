/** Available visual themes. */
export type Theme = 'light' | 'dark'

/** Screen coordinates the theme-change reveal animation should expand from. */
export type ThemeChangeOrigin = {
  readonly x: number
  readonly y: number
}
